<!DOCTYPE html>

<html>
  <head>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css" rel="stylesheet">
    <title>Address Book</title>
  </head>
  <body>
    <div class="container">

      {% if contacts is empty %}
        <div class="row">
          <h1>Welcome to your address book!</h1>

          <p class="lead">When you fill out the form, your contacts will be show below just like the example!</p>



          <hr>

          <div class="col-md-6">
            <h3>Add a contact</h3>
            <form action="/create_contact" method="post">
              <div class="form-group">
                <label for="name">Enter contact name</label>
                <input type="text" name="name" id="name" class="form-control">
              </div>
              <div class="form-group">
                <label for="phone">Enter contact phone</label>
                <input type="tel" name="phone" id="phone" class="form-control">
              </div>
              <div class="form-group">
                <label for="address">Enter contact address</label>
                <input type="text" name="address" id="address" class="form-control">
              </div>

              <button type="submit" class="btn btn-success">Add Contact</button>
            </form>
          </div>

          <div class="col-md-6">
            <h3>Search for a contact</h3>
            <form action="/search" method="post">
              <div class="form-group">
                <label for="search">Enter a search</label>
                <input type="search" name="search" id="search" class="form-control" placeholder="Enter a search here">
              </div>
              <button type="submit" class="btn btn-primary">Search</button>
            </form>
          </div>
        </div>

        <hr>

        <div class="row">
          <div class="col-md-6">

            <h3>Contact Name</h3>

            <p>
              Phone: (555)-555-5555
            </p>

            <p>
              Address: 1234 Example Lane, Portland, OR
            </p>

          </div>
        </div>

      {% else %}

        <div class="row">
          <h1>Address Book</h1>

          <p class="lead">
            A Simple place to store all your contacts!
          </p>

            <div class="col-md-6">
              <form action="/create_contact" method="post">
                <div class="form-group">
                  <label for="name">Enter contact name</label>
                  <input type="text" name="name" id="name" class="form-control">
                </div>
                <div class="form-group">
                  <label for="phone">Enter contact phone</label>
                  <input type="tel" name="phone" id="phone" class="form-control">
                </div>
                <div class="form-group">
                  <label for="address">Enter contact address</label>
                  <input type="text" name="address" id="address" class="form-control">
                </div>

                <button type="submit" class="btn btn-success">Add Contact</button>
              </form>
          </div>

          <div class="col-md-6">
            <h3>Search for a contact</h3>
            <form action="/search" method="post">
              <div class="form-group">
                <label for="search">Enter a search</label>
                <input type="search" name="search" id="search" class="form-control" placeholder="Enter a search here">
              </div>
              <button type="submit" class="btn btn-primary">Search</button>
            </form>
          </div>
        </div>

        <hr>

        <div class="row">

          {% for contact in contacts %}

            <div class="col-md-6">

              <h3>{{contact.getName()}}</h3>

              <p>
                Phone: {{contact.getPhone()}}
              </p>

              <p>
                Address: {{contact.getAddress()}}
              </p>

              <form action="/delete" method="post">
                <div class="form-group">
                  <input type="hidden" name="name" value="{{contact.getName()}}">
                  <input type="hidden" name="address" value="{{contact.getAddress()}}">
                  <input type="hidden" name="phone" value="{{contact.getPhone()}}">
                  <button name="button" value="homepage" type="submit" class="btn btn-warning">Remove</button>
                </div>
              </form>
            </div>

          {% endfor %}

        </div>

          <hr>

          <div class="row">
            <div class="col-md-12">
              <form action="/delete_contacts" method="post">
                <div class="form-group">
                  <button type="submit" class="btn btn-danger">DELETE ALL</button>
                </div>
              </form>
            </div>
          </div>

        </div>

      {% endif %}

      </div>
    </div>
  </body>
</html>
